[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "upm-mail-mcp"
version = "0.1.0"
description = "MCP server for IMAP/SMTP access to Universidad Politecnica de Madrid email"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12",
]

[project.optional-dependencies]
dev = [
    "pre-commit>=3.8",
    "pytest>=8.3",
    "pytest-cov>=5.0",
]

[project.scripts]
upm-mail-tools = "upm_mail_mcp.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 99
target-version = ["py311"]

[tool.isort]
profile = "black"
line_length = 99
known_first_party = ["upm_mail_mcp"]

[tool.pytest.ini_options]
pythonpath = ["src", "tests"]
testpaths = ["tests"]
addopts = "--cov=upm_mail_mcp --cov-report=term-missing"

[tool.coverage.run]
branch = true
source = ["upm_mail_mcp"]
omit = ["*/upm_mail_mcp/__main__.py"]

[tool.coverage.report]
fail_under = 80
show_missing = true
